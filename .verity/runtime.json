{
  "version": "0.12.0",
  "deployed_at": "2026-07-25T03:30Z",
  "rollback_from": "v0.11.0 (git checkout v0.11.0 + rebuild + restart; no migrations in v0.12.0; the worker timeout is additive/config-gated — NIGHTSHIFT_JOB_TIMEOUT_MS). Full rollback to pre-reMarkable: v0.9.1.",
  "environments": {
    "prod": {
      "tag": "v0.12.0",
      "host": "dev server (nsaf-dev-server / 3090-tuf, user systemd)",
      "webhook_url": "https://3090-tuf.taile0ffc4.ts.net/webhook",
      "rotation": "enabled (NIGHTSHIFT_ROTATION_ENABLED=true); stage-2 smoke passed 2026-07-06",
      "jobs": "enabled (NIGHTSHIFT_JOBS_ENABLED=true); stage-4 smoke passed 2026-07-06 (succeed + reconcile-adopt + kill + Webex notices)",
      "remarkable": "PUSH enabled (NIGHTSHIFT_REMARKABLE_ENABLED=true, NIGHTSHIFT_REMARKABLE_FOLDER=/NS-Inbox, RMAPI_BIN=~/.local/bin/rmapi); stage-19 smoke passed 2026-07-25. INBOX live: note-ingest job type (stage 20) consumes notes the reMarkable /Outbound watcher submits — verified end-to-end 2026-07-25 (handwritten note → watcher → note-ingest worker → composed doc → /NS-Inbox).",
      "job_timeout": "enabled (NIGHTSHIFT_JOB_TIMEOUT_MS default 2h; note-ingest 15m) — stage 21: a stalled `running` worker is killed + recorded `killed (timed out after Nm)` via the reconcile tick, restart-safe. Added after a note-ingest worker hung on a stalled model call.",
      "stages": "1-21 live: (1-18 spine/rotation/jobs/control/pipelines/promotions/perplexity-MCP) + reMarkable PUSH (19), note-ingest INBOX job type (20), per-job worker timeout (21)",
      "verified": "2026-07-25 ~03:30Z: v0.12.0 deployed, ACTIVE. reMarkable PUSH + INBOX round-trip verified live; worker-timeout reaper live (default 2h / note-ingest 15m). Off-network transport = reMarkable cloud API."
    }
  },
  "secret_locations": [
    "WEBEX_BOT_TOKEN, WEBEX_WEBHOOK_SECRET, WEBEX_OWNER_PERSON_ID, NIGHTSHIFT_AGENT_BIN @ ~/apps/nightshift-assistant/.env on the dev server (mode 0600)",
    "reMarkable cloud device token @ ~/.config/rmapi/rmapi.conf on the dev server (mode 0600) — used by RMAPI_BIN for the reMarkable PUSH capability; never in git, never in worker env"
  ],
  "notes": [
    "open threads: issue #33 (study health-check soft-404); upstream tg-skill nondeterminism (techguide-config.json not always written — nsaf repo, outside this project; daemon now tolerates it)"
  ]
}
