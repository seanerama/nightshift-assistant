# Status & Handoff

> Runtime/ops truth (framework-spec §4.6). Generated from `.verity/runtime.json`
> by the Release/Deploy Operator. Secret LOCATIONS only — never values.

**Live version:** 0.13.0
**Deployed at:** 2026-07-28T14:44Z (dark), app transport enabled 14:44Z, port 3779
**Rollback from:** v0.12.2 (git checkout + restart; migrations additive; db backup ~/backups/nightshift/nightshift-20260728-144248.db; app-transport kill: APP_TRANSPORT_ENABLED=false + restart — drill-verified 2026-07-28)

## Environments
- **prod:** {"tag":"v0.13.0","host":"dev server (nsaf-dev-server / 3090-tuf, user systemd)","webhook_url":"https://3090-tuf.taile0ffc4.ts.net/webhook","app_transport":"ENABLED (stage 24–29): tailnet-private http://100.110.222.42:3779/app/v1 (loopback + tailnet bind only, bearer NIGHTSHIFT_APP_TOKEN, 401-precedes-404, never Funneled); capabilities chat+files+mcp-tools+mcp-apps-ui; port 3779 because 3778 belongs to my-c-sweet; harness on prod 21/23 (2 fails = upstream 10s reply window, see v0.13.0 section)","rotation":"enabled (NIGHTSHIFT_ROTATION_ENABLED=true); stage-2 smoke passed 2026-07-06","jobs":"enabled (NIGHTSHIFT_JOBS_ENABLED=true); stage-4 smoke passed 2026-07-06 (succeed + reconcile-adopt + kill + Webex notices)","remarkable":"PUSH enabled (NIGHTSHIFT_REMARKABLE_ENABLED=true, NIGHTSHIFT_REMARKABLE_FOLDER=/NS-Inbox, RMAPI_BIN=~/.local/bin/rmapi); stage-19 smoke passed 2026-07-25. INBOX live: note-ingest job type (stage 20) consumes notes the reMarkable /Outbound watcher submits — verified end-to-end 2026-07-25 (handwritten note → watcher → note-ingest worker → composed doc → /NS-Inbox).","job_timeout":"enabled (NIGHTSHIFT_JOB_TIMEOUT_MS default 2h; note-ingest 15m) — stage 21: a stalled `running` worker is killed + recorded `killed (timed out after Nm)` via the reconcile tick, restart-safe. Added after a note-ingest worker hung on a stalled model call.","stages":"1-23 live: … stage 23 adds URL HANDLING to the conversational preamble (URL work is dispatched to fetch-capable workers, never dead-ended)","verified":"2026-07-25 19:42Z: v0.12.2 deployed, ACTIVE; URL HANDLING line confirmed in dist; session rotated 51292b5e→de76f23a; seed audited — stale paste-belief confined to 2026-07-25.md (not seed material; latest is -2 stub + operator note). Webex smoke (docs/smoke/stage-23.md steps 2–4) PENDING OWNER — only the owner can message the bot"}

## v0.13.0 — app transport LIVE on the tailnet (deployed + enabled 2026-07-28; certification PARTIAL)
- **Deployed dark, then enabled deliberately:** v0.13.0 active; migrations 0007/0008 applied (schema 8). App transport on **port 3779** — NOT the planned 3778: pre-flight found `my-c-sweet.service` (a separate Webex-fronted daemon) already on 127.0.0.1:3778 with its own pre-existing public Funnel route `/csweet/webhook`. Port was a config knob; no code change. Host .env: `APP_TRANSPORT_ENABLED=true`, `NIGHTSHIFT_APP_BIND=127.0.0.1,100.110.222.42`, `NIGHTSHIFT_APP_PORT=3779`; `NIGHTSHIFT_APP_TOKEN` generated on first enable (never printed).
- **Live evidence (docs/smoke/stage-29.md, filled 2026-07-28):** bind = loopback + tailnet IP only (`ss` evidence); 401-precedes-404 from another tailnet machine (real + fake path, bad token); manifest = exactly the four capabilities; REAL SSE round-trip (ack id 1 → reply id 2 from the live session, 7.4s); files/MCP/resource protocol checks live (five tools, status round-trip, resource read with `_meta["ui/tools"]` intact); Webex dual-run send leg proven via a real job-finish notice (Webex delivery + app `notice` event id 12, zero send failures); rollback drill executed (flag off → routes VANISH, daemon healthy → re-enabled → health 200); Funnel byte-identical before/after (routes: `/webhook` → 3777, pre-existing `/csweet/webhook` → 3778 [my-c-sweet's, not ours]).
- **⚠️ Harness on prod: 21/23 — exit 0 NOT achieved; certification NOT claimed.** The 2 failures (`outbox.reply`, `files.roundtrip`) are the harness v1.0.0's HARD-CODED 10s reply window vs real-model turn latency — no timeout knob exists. Not a daemon defect: the manual SSE round-trip shows a real reply in 7.4s, and the harness's own late messages received real replies (outbox ids 10–11) after its window closed. CI (instant agent stub) passes 23/23. Blocked on upstream: agent-app-contract needs a `--reply-timeout-ms` knob (issue filed; contract-v1.0.1 candidate). Re-run the harness against prod when it lands.
- **Env-path discrepancy RESOLVED (smoke step 0):** only `~/apps/nightshift-assistant` exists on the host — deploy-access.md's `~/nightshift-assistant/.env` line was the stale one; corrected.
- **✅ Owner Stage-0 exit DONE (2026-07-28 ~15:53Z, owner-performed from the Nightshift Client on the phone):** chat round-trip through the app door (after fixing the connection's "Owner person id" field — the client's `owner-nightshift` default 403s against the daemon's configured owner id, by design); then job `a3d4ef7e` ("stage-0 kill-me from the phone", type test) SUBMITTED from the jobs dashboard's form AND KILLED from the phone — `running → killed` in the daemon transition log via the `ui://nightshift/jobs@v1` resource's allowlisted `jobs_submit`/`jobs_kill` tools. The v0.13.0 app surface is owner-verified end to end.
- **Still PENDING OWNER:** (1) Webex INBOUND round-trip — one owner message to the bot in Webex confirms the old door's inbound leg under dual-run; (2) MCP Inspector GUI session per docs/smoke/stage-27/28 (optional — protocol-level equivalents verified via curl).

## Secret locations (names + on-disk locations only, never values)
- WEBEX_BOT_TOKEN, WEBEX_WEBHOOK_SECRET, WEBEX_OWNER_PERSON_ID, NIGHTSHIFT_AGENT_BIN @ ~/apps/nightshift-assistant/.env on the dev server (mode 0600)
- NIGHTSHIFT_APP_TOKEN (app transport bearer, distinct from NIGHTSHIFT_API_TOKEN) @ ~/apps/nightshift-assistant/.env on the dev server — generated by deploy.sh on first enable; rotate by deleting the line and re-running deploy
- reMarkable cloud device token @ ~/.config/rmapi/rmapi.conf on the dev server (mode 0600) — used by RMAPI_BIN for the reMarkable PUSH capability; never in git, never in worker env

## Coordination notes
- open threads: issue #33 (study health-check soft-404); upstream tg-skill nondeterminism (techguide-config.json not always written — nsaf repo, outside this project; daemon now tolerates it)
