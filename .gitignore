node_modules/
dist/
# Runtime state (SQLite lives here; never committed)
data/
# App-transport upload landing dir (Stage 26) — created in the daemon's cwd
# (the CI conformance job runs the daemon from the checkout root)
uploads/
*.tgz
.DS_Store
.env
.env.*
!.env.example
!.env.*.example
# Verity's derived state cache — never authoritative (framework-spec §5)
.verity-cache/
# Per-app deployment access (host + credential locations) — shared out-of-band,
# never committed. The committed pointer (.verity/deploy-access.README.md) stays.
.verity/deploy-access.md
